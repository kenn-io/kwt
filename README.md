# kwt website assets

This orphan branch stores binary assets for the kwt website. It is intentionally
kept separate from the source and documentation history and must not be merged
into `main`.

Stable asset URLs use the raw branch form:

```text
https://raw.githubusercontent.com/kenn-io/kwt/website-assets/<asset>
```
